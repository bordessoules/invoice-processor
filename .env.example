# Copy this to .env (gitignored) and fill in your real keys.

# OpenRouter — used by configs/openrouter-*.json
OPENROUTER_API_KEY=sk-or-v1-...

# LM Studio (optional — only needed if your LM Studio actually requires auth)
# OPENAI_API_KEY=not-needed
# OPENAI_BASE_URL=http://100.100.138.95:1234/v1
